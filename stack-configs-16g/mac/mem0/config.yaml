# ============================================
# MEM0 — Configuration
# Mémoire partagée entre LobeChat, SurfSense,
# Open Notebook, Paperclip
# ============================================

vector_store:
  provider: chroma
  config:
    host: host.docker.internal
    port: 8000
    collection_name: mem0

llm:
  provider: ollama
  config:
    model: llama3.2:3b
    base_url: http://host.docker.internal:11434

embedder:
  provider: ollama
  config:
    model: nomic-embed-text
    base_url: http://host.docker.internal:11434
